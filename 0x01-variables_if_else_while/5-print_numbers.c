#include <stdio.h>
/**
 * main - print all single digit number from base 10 starting from base 0.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
