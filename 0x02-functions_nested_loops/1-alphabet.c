#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0 (success).
 */
int main(void)
{
	int i;

	for (i = 97; i < 123; i++)
	{
		_putchar(i);
	}
	print_alphabet();
	_putchar('\n');
	return (0);
}
